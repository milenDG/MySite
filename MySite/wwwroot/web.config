<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <system.webServer>
    <rewrite>
      <rules>
        <rule name="Redirect rquests to custom domain" stopProcessing="true">
          <match url="(.*)" />
          <conditions logicalGrouping="MatchAny">
            <add input="{HTTP_HOST}" pattern="^mysite\.azurewebsites\.net$" />
            <add input="{HTTP_HOST}" pattern="^\milengeorgiev\.co\.uk$" />
          </conditions>
          <action type="Redirect" url="https://www.milengeorgiev.co.uk/{R:0}" />
        </rule>
      </rules>
    </rewrite>
  </system.webServer>
</configuration>
